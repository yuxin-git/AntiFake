<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.antifake">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AntiFake">
        <activity android:name=".DealerInventorySearchActivity"></activity>
        <activity android:name=".DealerInfRecordActivity" />
        <activity android:name=".ManuInventorySearchActivity" />
        <activity android:name=".ManuInfRecordActivity" />
        <activity android:name=".BrandDeallistSearchActivity" />
        <activity android:name=".BrandInventorySearchActivity" />
        <activity android:name=".SearchInfomationActivity" />
        <activity android:name=".BrandMainActivity" />
        <activity android:name=".ManufacturerMainActivity" />
        <activity android:name=".DealerMainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AntiFake.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>